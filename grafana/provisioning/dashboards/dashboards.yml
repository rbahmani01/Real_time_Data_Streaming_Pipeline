apiVersion: 1

providers:
  - name: 'default'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
      # allowUiUpdates: true   # uncomment if you want to edit dashboards in UI and save back
